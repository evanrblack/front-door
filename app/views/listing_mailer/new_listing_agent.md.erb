There is a new listing in your area:

[<%= @listing.address %>](<%= listing_url(@listing) %>)

If you are interested in helping <%= @client.name %> to sell this property, click the 'CLAIM THIS LISTING' button at top of the page.